#This is a git repo
**Second** commit test

git add --all 
git commit -am "<commit message>"
git push

git add -A
git commit -m "some_message"

git remote add origin https://github.com/user_name/Mytest.git //seulement 1 fois pour créer la branche

git push origin master

git remote add origin https://github.com/DonDiegoAA/phpnoframework.git
git push -u origin master

//git add -A

================

git commit -am "<commit message>"
git pull
git push
